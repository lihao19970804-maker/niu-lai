[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vigilmesh"
version = "0.1.0"
description = "NiuLai (牛来): early-stage open-source AI agent runtime for monitoring, research, alerts, and reports"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "NiuLai contributors" }]
keywords = ["agents", "monitoring", "scheduler", "alerts", "research"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = []

[project.scripts]
vigilmesh = "vigilmesh.cli:main"

[project.urls]
Homepage = "https://github.com/lihao19970804-maker/niu-lai"
Repository = "https://github.com/lihao19970804-maker/niu-lai"
Issues = "https://github.com/lihao19970804-maker/niu-lai/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.coverage.run]
source = ["vigilmesh"]
