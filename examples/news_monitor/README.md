# News monitor example

This deterministic sample stands in for a public RSS, search, or news API
provider. No account, cookie, or private feed is used.

```bash
vigilmesh run --config examples/news_monitor/config.yaml --json
```
