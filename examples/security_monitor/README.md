# Security monitor example

This local sample demonstrates event triage using fictional authentication
telemetry. It performs no scans and does not execute remediation commands.

```bash
vigilmesh run --config examples/security_monitor/config.yaml --json
```
