# Market monitor example

This example uses synthetic, non-actionable observations to show how a public
market feed could plug into VigilMesh. It is not trading software, investment
advice, or a connection to a brokerage account.

```bash
vigilmesh run --config examples/market_monitor/config.yaml --json
```

